/** @deprecated Import from buildAnswerPrompt.ts — re-export for existing imports. */
export {
  buildChatCompletionRequest,
  buildAnswerPrompt,
  parseSelectionRecord,
  formatPageContextBlock,
  resolveUserMessage,
} from "./buildAnswerPrompt.js";
